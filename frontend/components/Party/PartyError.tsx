import { StyleSheet, Text, View } from 'react-native'

const PartyError = () => {
    return (
        <View>
            <Text>PartyError</Text>
        </View>
    )
}

export default PartyError

const styles = StyleSheet.create({})